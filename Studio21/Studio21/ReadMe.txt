Studio 21 ReadMe.txt
1. Austin Bick, Brendan Long, Josh Pearlson
2. If we don't declare it, then any derived class cannot override the destructor.
3. It allows it to be used for any file system. We don't actually declare anything related to the simple file system in particular, but 
   rather abstract file system which allows any system to be used.
4. We created the file factory, file system, the touch command, and the command prompt.
   We then tested quitting, the help commmand, and the touch command and everything worked as expected. The file was created successfully.